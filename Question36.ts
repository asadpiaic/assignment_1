function make_shirt(size: string, message: string) {
  console.log(`You ordered a ${size} t-shirt with the message "${message}" printed on it.`);
}

// Example usage
make_shirt('medium', 'Hello, World!');
